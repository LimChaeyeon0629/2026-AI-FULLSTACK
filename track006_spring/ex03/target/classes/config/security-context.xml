<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd
		http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-4.2.xsd">

	<!-- 순서: request / token / provider / 로그인 정보 -->
	<!-- 암호화 -->
	<bean 	id="passwordEncoder"
			class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder" />
	<!-- 로그인성공 -->
	<bean 	id="successHandler"
			class="com.the703.security.LoginSuccessHandler" />
	<!-- 로그인실패 -->
	<bean 	id="deniedHandler"
			class="com.the703.security.DeniedHandler" />
	<!-- 유저정보
	1. permitALL : 아무나 모두 접근가능
	2. hasRole( 'ROLE_MEMBER' ) : ROLE_MEMBER 라는 권한을 가지고 있을 때 접근가능
	3. isAuthenticated() : 인증된 사용자 (로그인 했음)
	구체적인 패턴을 위에, 포괄 패턴은 아래 -->
	<bean 	id="customUserDetailsService"
			class="com.the703.security.CustomUserDetailsService" />
	
	<security:http>
		<security:intercept-url  pattern="/board/**" 		access="permitAll" />
		<security:intercept-url  pattern="/security/all" 	access="permitAll" />
		<security:intercept-url  pattern="/security/member" access="hasRole( 'ROLE_MEMBER' )" />
		
		<security:intercept-url  pattern="/security/join" 	access="permitAll" />
		<security:intercept-url  pattern="/security/login" 	access="permitAll" />
		<security:intercept-url  pattern="/security/**" 	access="isAuthenticated()" />
		
		<security:form-login 	login-page="/security/login"
								authentication-success-handler-ref="successHandler" 
								authentication-failure-forward-url="/security/fail" />

		<security:logout				logout-url="/security/logout"
								logout-success-url="/security/login"
								invalidate-session="true" />
	</security:http>
	
	<!-- ver-2 user-service-ref & 암호화 -->
	<security:authentication-manager>
		<security:authentication-provider user-service-ref="customUserDetailsService">
			<security:password-encoder ref="passwordEncoder" />
		</security:authentication-provider>
	</security:authentication-manager>
	
	<!-- db에서 authorities(권한) 설정 (user-service-ref) -->
	<!-- ver-1 authentication-manager -->
<!-- 	<security:authentication-manager>
		<security:authentication-provider >
			<security:user-service>
				<security:user name="member" password="member" authorities="ROLE_MEMBER"/>
			</security:user-service>
		</security:authentication-provider>
	</security:authentication-manager> -->

</beans>
